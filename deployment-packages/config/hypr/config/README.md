# Device-specific configuration

This folder holds the machine-local Hyprland module. Copy the deployed
template, then set the monitor, backlight, and optional AQ_DRM variables:

```bash
cp $HOME/.config/hypr/config/device{.dist,}.lua
$EDITOR $HOME/.config/hypr/config/device.lua
```
