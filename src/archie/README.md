# Archie Application

This package implements the `archie` command-line interface, GTK settings UI,
status-notifier applet, system policy operations, and shared SQLite-backed
state. Static application assets and bundled Waybar themes live alongside the
Python modules.

For installed package contents and system integration, see the
[Arch Linux integration architecture](../../docs/architecture/ARCH_LINUX_INTEGRATION.md).
